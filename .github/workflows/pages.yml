name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Gleam
        uses: erlef/setup-beam@v1
        with:
          otp-version: "28"
          gleam-version: "1.18.1"
          rebar3-version: "3"

      - name: Download dependencies
        run: gleam deps download

      - name: Build json_to_json_converter app
        run: cd json_to_json_converter && gleam run -m lustre/dev build

      - name: Fix asset paths for sub-path deployment
        run: sed -i 's|href="/|href="./|g; s|src="/|src="./|g' json_to_json_converter/dist/index.html

      - name: Add Umami analytics
        run: |
          sed -i 's|</head>|<script defer src="https://analytics.merciless.me/script.js" data-website-id="8e6be986-64a0-434f-9a9e-e246780414ec"></script></head>|' \
            json_to_json_converter/dist/index.html

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./json_to_json_converter/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
